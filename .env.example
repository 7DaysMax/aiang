# Optional local runtime credentials. Leave blank until using your own account.
# Copy to .env locally if needed; never commit .env or real credentials.
# Building the application does not require either key.
DEEPSEEK_API_KEY=
OPENROUTER_API_KEY=
